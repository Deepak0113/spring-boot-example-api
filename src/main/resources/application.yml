server:
  port: 8080

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/SpringBootExampleDB"
    username: "root"
    password: "Deepak@123"
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet